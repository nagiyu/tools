export interface MyTickerSummaryDataType {
  tickerId: string;
  exchangeId: string;
  tickerName: string;
  exchangeName: string;
  currentQuantity: number;
  totalCost: number;
  averagePrice: number;
}